# Notebooks

Jupyter Lab mounts this directory at `/app/notebooks`. Add notebooks here to have
them available in the Jupyter service (`http://localhost:8080`).

The downloaded CSV data is shared with the app and appears at `data/` inside the
notebook root (i.e. `/app/notebooks/data/house_prices`).
